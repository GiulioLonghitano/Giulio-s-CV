import React from 'react';
import { Button} from 'antd';

const Footer: React.FC = () => (
  <div style={{background: 'red'}}>
    <p>Giulio's CV Website</p>
    <Button>Click me</Button>
  </div>
);

export default Footer;